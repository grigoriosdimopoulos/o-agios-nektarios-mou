{
  "name": "agios-nektarios-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for the Agios Nektarios village app",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "test:rules": "cd .. && firebase emulators:exec --only firestore --project demo-rules-check 'node rules.test.mjs'"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
