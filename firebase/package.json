{
  "name": "agios-nektarios-firebase",
  "private": true,
  "description": "Firestore security rules and their tests",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-rules-check 'node rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
